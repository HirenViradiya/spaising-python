1. cd into **_ frontend _** folder
2. Run npm install to download all required dependecy.
3. Create .env file and copy .env.example file content and paste into .env file
4. run npm start to run front end project
